import React from 'react';
import { DatePicker } from 'antd';

const { RangePicker } = DatePicker;

const HighestVolumeForm = () => {
  return <RangePicker style={{ width: '350px' }} />;
};

export default HighestVolumeForm;
