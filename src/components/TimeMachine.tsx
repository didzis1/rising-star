import React from 'react';
import { Typography } from 'antd';

const { Text } = Typography;

const TimeMachine = () => {
  return (
    <div>
      <Text>Time machine screen</Text>
    </div>
  );
};

export default TimeMachine;
